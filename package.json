{
  "name": "komaru-corp-a11y",
  "description": "a11yを学習するために作成された、架空のWeb制作会社のサイトです。",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --open",
    "build": "vite build",
    "preview": "pnpm build && vite preview",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "preinstall": "npx only-allow pnpm",
    "postinstall": "cp -r node_modules/normalize.css/normalize.css src/styles",
    "build-gh-pages": "git checkout bad && pnpm build && mv dist dist_bad && git checkout enhanced && pnpm build && mv dist dist_enhanced && git checkout gh-pages && rm -rf bad enhanced && mv dist_bad bad && mv dist_enhanced enhanced"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "1.60.0",
    "vite": "8.0.16"
  },
  "dependencies": {
    "normalize.css": "8.0.1"
  },
  "packageManager": "pnpm@11.4.0",
  "engines": {
    "node": "24.16.0",
    "pnpm": "11.4.0"
  }
}
